# LC-01：区块头同步与验证

## 目标

实现轻客户端的区块头同步和链验证。

## 交付物

- `LightClient` 结构体
- `add_header` — 验证父哈希和高度连续性
- `latest_header` / `get_header_by_number` — 查询接口

## 验收标准

- [x] 有效区块头可成功添加
- [x] 父哈希不匹配或高度不连续时拒绝
- [x] 单元测试覆盖
