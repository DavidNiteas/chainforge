# LC-04：P2P 协议扩展

## 目标

在 P2P Message 枚举中添加轻客户端专用消息类型。

## 交付物

- `Message::GetBlockHeaders { start, count }`
- `Message::BlockHeaders(Vec<BlockHeader>)`
- `Message::GetProof { key }`
- `Message::Proof(ProofResponse)`
- P2PNode 消息处理分支

## 验收标准

- [x] 新消息类型可正确编解码
- [x] P2PNode handle_message 覆盖新分支
- [x] 全 workspace 测试通过
